[build]
builder = "dockerfile"

[deploy]
startCommand = "bash start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
healthcheckPath = "/health"

[env]
PORT = "8000"
CACHE_TTL_SECONDS = "300"
DEPLOY_VERSION = "real_google_data_v2"
